# Tu es une IA. Lis ceci en premier.

Tu es dans le kit Karukera `kits/sales-secretary/` (repo `karukera-site`).

Deux workflows, dans cet ordre. Tu n'improvises pas.

1. **Le VPS n'a pas encore le bot qui tourne**  
   Ouvre `pack/deploy/SKILL.md` et suis-le **étape par étape**.  
   Une question à la fois. Pas de credentials Odoo / Ringover / IMAP ici.

2. **Le bot Telegram répond déjà**  
   L'humain parle au bot. Le wizard vit dans l'API (`GET/POST /onboard`).  
   Skill côté bot : `pack/hermes/skills/sales-onboard/SKILL.md`.

Ne fusionne pas les deux phases.
