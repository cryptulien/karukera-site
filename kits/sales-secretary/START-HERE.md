# Karukera · Kit secrétaire commercial — START HERE

Appliance Hermes : mails + appels Quicktalk/Ringover → cartes Telegram → Odoo (après `ok`).

Deux phases. Tu n’en fusionnes aucune.

## 1. Tu es une IA qui déploie le VPS

Lis `FOR-AI.md`, puis suis **exactement** `pack/deploy/SKILL.md`.  
Une question à la fois. Pas d’Odoo / Ringover / IMAP ici.

## 2. Le bot Telegram répond déjà

L’humain parle au bot. Le wizard est `GET/POST /onboard` (15 étapes figées).  
Tu n’inventes pas la question suivante.

## 3. Ensuite

Les cartes arrivent toutes seules. Le client répond `ok` / `ignore` / une modif.  
Rien n’écrit dans Odoo avant le `ok`.

Détail produit : `docs/SPEC.md`. Installation manuelle : `README.md`.
